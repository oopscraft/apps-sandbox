rootProject.name = 'apps-sandbox'
include 'apps-sandbox-core'
include 'apps-sandbox-web'
include 'apps-sandbox-batch'

if(System.getProperty("includeSubmodule") == "true") {
    includeBuild("../")
}
